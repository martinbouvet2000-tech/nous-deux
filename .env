# Secrets removed from repository for security.
# Create a local .env.local (not committed) with the following variables:
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
